Analyze the attached image and extract all text. Then, follow these instructions:
1. Identify the source language of the extracted text.
2. Translate the text into English and Vietnamese.
3. Special Rule: If the original text is already in English, the "English" output should be a proofread and corrected version. If the original text is already in Vietnamese, the "Vietnamese" output should be a proofread and corrected version.

Your final output MUST be a single block of HTML code, structured and styled exactly as defined below. Do not wrap the output in markdown fences (like ```html).

- The entire response must be contained within a single parent `<div>`.
- Each language section (Original, English, Vietnamese) must be a `div` with the class `section`.
- Each section must have an `<h2>` for the title and a `<p>` for the content.
- Preserve all line breaks from the extracted text by converting them to `<br>` tags inside the `<p>` elements.
- Ensure any special characters within the text content (like < or >) are properly HTML-encoded to avoid breaking the layout.

Here is the exact HTML structure to use:

<style>
    .ai-result-panel { font-family: 'Segoe UI', sans-serif; line-height: 1.6; }
    .ai-result-panel .section { 
        background-color: #f7f8fa; 
        border-left: 4px solid #4267B2; 
        margin-bottom: 15px; 
        padding: 10px 15px; 
        border-radius: 4px;
    }
    .ai-result-panel .section-title { 
        margin: 0 0 5px 0; 
        font-size: 1.1em; 
        color: #1d2129; 
    }
    .ai-result-panel .section-content { 
        margin: 0; 
        color: #4b4f56; 
        white-space: pre-wrap; /* Helps preserve whitespace and line breaks */
    }
</style>
<div class="ai-result-panel">
    <div class="section">
        <h2 class="section-title">Original</h2>
        <p class="section-content">{The raw, extracted text with line breaks as <br> and HTML-encoded content}</p>
    </div>
    <div class="section">
        <h2 class="section-title">English</h2>
        <p class="section-content">{The English version with line breaks as <br> and HTML-encoded content}</p>
    </div>
    <div class="section">
        <h2 class="section-title">Vietnamese</h2>
        <p class="section-content">{The Vietnamese version with line breaks as <br> and HTML-encoded content}</p>
    </div>
</div>